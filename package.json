{
  "name": "tauque",
  "version": "1.0.0",
  "main": "src/index.js",
  "bin": "bin/install.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.js --bundle --minify --format=cjs --platform=node --target=node8 --outfile=dist/esbundle.js",
    "dev": "nodemon --watch src --exec \"npm run build\""
  },
  "author": "",
  "license": "ISC",
  "keywords": [],
  "description": "",
  "dependencies": {
    "ansi-colors": "^4.1.1",
    "ansi-escapes": "^4.3.1",
    "esbuild": "^0.8.16",
    "node-watch": "^0.7.0",
    "prompts": "^2.4.0"
  }
}
